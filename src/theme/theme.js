import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  fonts: {
    body: "'Roboto Mono', monospace, -apple-system",
    heading:"'Roboto Mono', monospace, -apple-system", 
    mono: "'Roboto Mono', monospace, -apple-system",
  }
})

export default theme
